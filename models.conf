# Monitors to auto-scale. One per line:
#   vendor:model:panel-resolution
# vendor/model are the decimal EDID ids. Find yours with BetterDisplay
# (display info) or:  ioreg -lw0 | grep -A9 DisplayAttributes
# Monitors NOT listed here are never touched.
# After editing, restart the daemon:  launchctl kickstart -k gui/$(id -u)/com.hidpiscale

4268:41413:1920x1080   # DELL P2422H
